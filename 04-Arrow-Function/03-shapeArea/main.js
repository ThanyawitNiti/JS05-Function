// บรรทัดที่มี \* , \*\*ให้ผลลัพธ์เป็นอะไร เพราะอะไร

// ```js
// const square = (n) => {
//   n ** 2;
// };

// console.log(square(7)); // * 49 เมื่อใส่ 7 ลงไปใน n ซึ่งเป็น parameter และฟังก์ชันนำ n ไปยกกำลังสอง เมื่อได้ผลลัพธ์ก็นำไปใส่ไว้ในตัวแปร square
// ```

// ```js
// const calcCircleArea = (r) => Math.PI * r ** 2;
// console.log(calcCircleArea(3)); // ** นำเลข 3 ซึ่งเป็น input ไปไว้ใน r และ นำ r ไปคำนวรณใน ฟังก์ชัน  3.14 * 3 **2 ; และนำผลลัพธ์ไปไว้ในตัวแปรที่ชื่อ calcCircleArea
// ```
