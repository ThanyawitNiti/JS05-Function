// โค้ดทั้งสองชุดด้านล่างทำงานได้เหมือนกันหรือไม่

// ```js
// const logName = (name) => console.log(name);
// const logName = name => console.log(name);
// ```
// ไม่เหมือนกัน เพราะ parameter name ต้องใส่ ( ) ด้วย ซึ่งจะทำให้โค้ดด้านล่างไม่ทำงาน